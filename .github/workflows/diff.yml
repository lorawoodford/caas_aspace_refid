name: Diff modified views

on:
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/views/**'
  push:
    paths:
      - 'frontend/views/**'

jobs:
  diff:
    uses: Smithsonian/caas-aspace-services/.github/workflows/diff.yml@main
